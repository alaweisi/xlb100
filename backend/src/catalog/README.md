# Catalog Module — Phase 3

City-scoped service directory (not transactions).

- `catalogRepository` — city-scoped category / item / sku queries
- `GET /api/catalog` — enabled catalog for request cityCode

**Rules:** no nationwide fallback · no `__global__` · demo seed only until formal categories approved
