# @xlb/dashboard

喜乐帮总部只读实时运营大屏，默认入口 `/dashboard/`，设计验收视口
`1920 × 1080`。

大屏每 15 秒读取一次 `/api/dashboard/realtime`，45 秒未获得新快照时显示
“数据已过期”，120 秒后显示“连接中断”。最近一次可信快照会保留，但不会继续
显示为实时数据。

展示范围仅限订单、支付、派单、履约、售后和客服的聚合统计；响应契约明确禁止
姓名、电话、地址、消息正文及师傅精确位置。
