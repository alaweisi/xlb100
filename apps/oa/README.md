# @xlb/oa

总部 OA 桌面网页。它复用 Admin 的真实业务页面与 API 契约，但采用宽屏、键盘和批量工作流优先的独立布局。

OA 使用专用 `/api/auth/oa/*` OTP、独立 `xlb.oa.*` 会话和 `appType=oa` 令牌。只有 `admin` 角色且拥有 `__global__` 总部范围的账号可以登录；进入业务页面前仍须选择真实城市，所有数据与操作继续按城市隔离并进入既有审计链路。
