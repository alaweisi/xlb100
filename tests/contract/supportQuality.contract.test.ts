import{describe,expect,it}from"vitest";import{createSupportQualityReviewRequestSchema,createSupportQualityRubricRequestSchema,submitSupportCsatRequestSchema,outboxEventTypeSchema}from"@xlb/validators";
describe("Phase 24F quality contract",()=>{it("rejects invalid and unknown CSAT fields",()=>{expect(submitSupportCsatRequestSchema.safeParse({score:6,idempotencyKey:"quality-key"}).success).toBe(false);expect(submitSupportCsatRequestSchema.safeParse({score:5,idempotencyKey:"quality-key",requesterId:"forged"}).success).toBe(false)});it("requires rubric weights total 100 and bounded review scores",()=>{expect(createSupportQualityRubricRequestSchema.safeParse({name:"Q",criteria:[{key:"a",weight:60,maxScore:5}]}).success).toBe(false);expect(createSupportQualityReviewRequestSchema.safeParse({targetType:"ticket",targetId:"ticket-1",rubricVersionId:"v-1",criterionScores:{a:11},idempotencyKey:"review-key"}).success).toBe(false)});it("closes Outbox event set",()=>{expect(outboxEventTypeSchema.parse("support.csat.submitted")).toBe("support.csat.submitted");expect(outboxEventTypeSchema.parse("support.quality.reviewed")).toBe("support.quality.reviewed")})});
