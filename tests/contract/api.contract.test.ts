import { describe, it, expect } from "vitest";

describe("api contract", () => {
  it.todo("Phase 1: customer API contract");
  it("placeholder passes", () => {
    expect(true).toBe(true);
  });
});
