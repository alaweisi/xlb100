$ErrorActionPreference='Stop';$root=Split-Path -Parent $PSScriptRoot;$paths=@('backend/src/support/quality','db/migrations/053_phase24f_support_quality.sql');$text=($paths|ForEach-Object{Get-ChildItem (Join-Path $root $_) -File -Recurse|Get-Content -Raw})-join "`n";if($text-match '(?i)(INSERT\s+INTO|UPDATE|DELETE\s+FROM)\s+(orders|payments|dispatch_|workers|worker_|aftersale_|ledger_|settlement_|refund_)'){throw 'Phase24F writes protected domain'};if(Test-Path (Join-Path $root 'db/migrations/024*')){throw 'migration 024 is permanent gap'};Write-Host 'Phase 24F boundaries PASS'
