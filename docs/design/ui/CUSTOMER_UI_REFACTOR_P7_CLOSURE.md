# 顾客端全业务切片设计系统统一与视觉重构 — P7 最终收口

> 日期：2026-07-22
>
> 范围：仅 `@顾客端`
>
> 唯一视觉真相：`docs/design/ui/references/customer-home-visual-truth.png`
>
> 结论：**CLOSED — Gate 6 / Gate 7 PASS**

## 1. 收口结论

顾客端 9 个页面载体、44 个执行切片已完成主页设计语言继承、关键业务状态覆盖和运行态复验。这里的“继承”是复用暖白底、墨绿/陶橙层级、功能层 Liquid Glass、苹果式服务卡片、语义 3D 服务图像、五项底栏、安全区与 44px 触控规则，不是把主页布局复制到业务页面。

最终缺陷口径为 P0=0、P1=0、P2=0、P3=1。唯一 P3 是售后投诉记录中的“转入客服跟进”在窄按钮中换行偏碎；文字完整、动作可用、无遮挡，不阻断 P7。

清单中共有 45 个勾选项，其中 `CUST-DS-001` 是主页视觉真相与继承规则本身，是 44 个执行切片的上游权威，不计入执行切片数量。

## 2. P6 三路串行归入

| 顺序 | QA lane | 原提交 | 集成提交 | 覆盖 | 首轮结果 |
| --- | --- | --- | --- | --- | --- |
| A | A6 发现/下单 | `b0531017` | `a88a0917` | 首页、服务、下单、优惠券 | P1=1、P2=1，证据完成但门禁失败 |
| B | B6 订单/售后 | `b3d352fa` | `9fb0de9d` | 订单、支付、评价、售后 | P0/P1/P2=0，P3=1 |
| C | C6 客服/账户 | `368b5449` | `9873ffde` | 客服、通知、我的、地址 | P0/P1/P2=0 |

三份提交都只包含各 lane 的 QA 配置、测试、报告和截图；没有把各自工作树中的业务源、锁文件或其它继承改动带入正式集成线。

## 3. A6 阻断修复

集成提交 `0895c1c2` 完成两项关门校正：

- 下单表单、数量步进和错误恢复按钮从 36px 提升到至少 44px；P7 强制回归覆盖 25 个场景，`undersizedTargets=0`。
- 深链提示从永久进行时“正在恢复”改为“已读取链接中的服务或优惠参数”，同时保留“最终以服务端返回为准”的权威边界，不声明业务成功。

P7 A6 测试新增可配置证据目录、完成态文案断言和 44px 强制门禁；B6/C6 也支持写入独立 P7 证据目录，避免改写 P6 历史快照。

## 4. 九载体证据矩阵

| 页面载体 | ready/default | 最高风险状态 | 主页语言对照 | P7 证据根 |
| --- | --- | --- | --- | --- |
| 首页 | 320/390/430 | 目录失败、真实 API partial | Round 09 + A6 | `phase25/evidence/customer/p7-discovery-order-regression/` |
| 服务目录 | 320/390/430 | 搜索无结果、SKU 深链 | A6 | 同上 |
| 创建订单 | 服务/地址/时间/确认 | 报价失败、深链恢复 | A6 | 同上 |
| 优惠券 | 320/390/430 空态 | API 失败与重试 | A6 | 同上 |
| 订单 | 320/390/430 | partial、支付入口、评价 | B6 | `phase25/evidence/customer/p7-orders-aftersale-regression/` |
| 售后 | 320/390/430 | 履约证据、服务端确认 | B6 | 同上 |
| 客服 | 工单/详情/会话 | closed、realtime recovery | C6 | `phase25/evidence/customer/p7-support-account-regression/` |
| 通知 | 收件箱 ready | 409 冲突恢复 | C6 | 同上 |
| 我的/地址 | 账户 ready | 受保护地址编辑 | C6 | 同上 |

## 5. 最终验证

| 门禁 | 结果 |
| --- | --- |
| Customer Home 浏览器回归 | 4/4 PASS；Round 09 证据检查 PASS |
| A6 P7 浏览器强制回归 | 1/1 PASS；25 场景；320/390/430；undersized target 0 |
| B6 P7 浏览器回归 | 2/2 PASS |
| C6 P7 浏览器回归 | 5/5 PASS |
| Customer 聚焦单测 | 17 文件 / 87 测试 PASS |
| Customer typecheck / lint | PASS / PASS |
| Customer 依赖拓扑构建 | `@xlb/types`、`@xlb/ui`、`@xlb/api-client`、`@xlb/customer` PASS |
| Phase25 设计权威门禁 | PASS |
| Phase25 Customer 材质配方门禁 | 21/21 PASS |
| diff / lean-risk | PASS；本批为 ordinary |

2026-07-22 的发布前修复已清零原记录的 5 个失败，且没有抬高冻结基线：Customer 尺寸硬编码由 `194` 降至 `35 ≤ 43`，字体硬编码由 `126` 降至 `0 ≤ 1`，数字层级由 `4` 降至 `0`。Phase27/28 检查改为解析当前 Phase 表并继续强制“生产阻断”事实，不再依赖 Phase14 的旧固定文案；通知到订单的检查同时识别统一安全深链构造器。修复后的仓库级全量回归为 199/200 文件、621/622 测试通过，1 项按环境条件跳过；typecheck、lint、build、Gate1A/Gate1B 及 A6/B6/C6 浏览器回归全部通过。

## 6. 范围与发布边界

- 未进入师傅端、后台管理端、OA 或 Dashboard 的继承施工。
- 未修改后端业务语义、数据库、支付/退款/账本、共享业务契约或正式服务目录。
- 推荐服务与附近师傅继续使用诚实空态；没有虚构师傅、距离、可接单状态、优惠或业务成功。
- 未 push、deploy、tag、访问生产数据或调用真实 Provider。
- 自动化与截图证明本轮列出的键盘、焦点、forced-colors、reduced-motion、无溢出和触控门禁；不外推为完整屏幕阅读器走查或完整 WCAG 认证。
