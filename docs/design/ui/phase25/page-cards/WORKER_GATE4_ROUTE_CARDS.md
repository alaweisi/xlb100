# Phase 25 Gate 4 — Worker Route Cards

Gate authorization: global Phase 25 authorization dated 2026-07-12. All cards use the existing authenticated Worker client and must not invent task, earnings, location, or certification facts.

| route | visual source | workflow / API source | states | actions | city / role / audit constraints | components | viewport | source screenshot | implementation screenshot | design QA |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/worker/` | `Worker / GrabHall / Online` and `Paused` (`1:1515`, `1:1647`) | `GET /api/worker/task-pool`; accept/reject/timeout client bindings | loading, empty, paused, online, eligible, blocked, offer, error | refresh; accept/reject only when API state allows; dev-only timeout simulation | authenticated worker + city query; eligibility is API-owned; accept is idempotent/audited | `MobileShell`, task cards, `StatusTag`, `BottomNav` | 390×844 | `frames/worker/worker_grabhall_online_1-1515.png` | pending browser capture | pending |
| `/worker/tasks` | `Worker / Tasks / Accepted` (`1:2452`) | `GET /api/worker/fulfillments` | loading, empty, assigned, started, completed, error | refresh; open detail | authenticated worker and service city; no local lifecycle mutation | task list, state tag, bottom nav | 390×844 | Figma frame inventory | pending browser capture | pending |
| `/worker/tasks/:id` | `Worker / TaskDetail / InProgress` (`1:2543`) | fulfillment detail, start/complete and Phase 18 evidence APIs | guarded, loading, actionable, uploading, completed, error | start, complete, upload evidence, refresh | owner/city/state guarded by API; evidence provider truth stays local/mock | detail, `ActionDock`, timeline, upload control | 390×844 | Figma frame inventory | pending browser capture | pending |
| `/worker/repairs` | Worker aftersale/task visual system | repair-order list/start/complete APIs | empty, assigned, started, completed, error | refresh, start, complete with note | authenticated worker; repair lifecycle and audit remain API-owned | task cards, form field, status tag | 390×844 | Figma task-system authority | pending browser capture | pending |
| `/worker/wallet` | `Worker / Income / Default` (`1:2742`) | receivable balance, bank account and withdrawal APIs | loading, empty, balance, submitting, reviewing, paid/rejected, error | reload, add bank account, request withdrawal | worker/city scope; never claim payout execution; no fabricated earnings | cards, fields, action dock | 390×844 | Figma frame inventory | pending browser capture | pending |
| `/worker/support` | Figma-derived Worker support | Phase 24 ticket/conversation/CSAT APIs | empty, open, realtime, resolved, error | create/comment/message/reopen/rate | authenticated requester and ticket ownership; messages remain durable API facts | support workspace, fields, status tag | 390×844 | Flow map + role recipe | pending browser capture | pending |
| `/worker/profile` | `Worker / Mine / Default` (`1:2811`) | worker location/settings APIs | loading, sharing on/off, valid/invalid location, error | reload, save location | exact location private; city and worker identity come from session/API | form fields, guardrail, status tag | 390×844 | Figma frame inventory | pending browser capture | pending |
| `/worker/certification` | `Worker / Mine / Default` (`1:2811`) | certification submission API | not-submitted, submitted, approved, rejected, error | submit certification | worker identity/city; approval status is never locally inferred | form, status tag, notice/error | 390×844 | Figma frame inventory | pending browser capture | pending |

Implementation baseline: `apps/worker/src/app/App.tsx`, `apps/worker/src/pages/`, and `@xlb/api-client`. The role theme is mounted at `apps/worker/src/main.tsx`; it is visual-only and makes no campaign or workflow decision.
