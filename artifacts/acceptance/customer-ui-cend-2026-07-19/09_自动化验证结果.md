# 自动化验证结果

验收日期：2026-07-19

## 浏览器设计 QA

- 视口：`390 × 844`
- 捕获状态：预约地址、预约成功、首页就绪、服务列表就绪、首页加载、服务列表为空、服务列表错误。
- 横向溢出：全部为 `false`。
- 小于 44px 的可见交互目标：`0`。
- 非预期控制台错误：`0`。
- 错误状态中的模拟 `503` 请求单独记录为预期错误。
- 最终结果：`passed`。

## 单元与组件测试

执行命令：

```powershell
pnpm exec vitest run tests/unit/customerNavigationShell.test.tsx tests/unit/customerOrderCreatePage.test.tsx tests/unit/customerProfileOperationsPage.test.tsx tests/unit/phase24bSupportPages.test.tsx tests/unit/phase25CustomerMaterialRecipe.test.ts tests/unit/phase27dNotificationPages.test.tsx tests/unit/phase29MarketingSurfaces.test.tsx
```

结果：

- Test Files：`7 passed`
- Tests：`43 passed`

## 类型、Lint 与构建

以下检查全部通过：

```powershell
pnpm --filter @xlb/customer typecheck
pnpm --filter @xlb/ui typecheck
pnpm --filter @xlb/customer lint
pnpm --filter @xlb/ui lint
pnpm exec turbo run build --filter=@xlb/customer...
git diff --check
```

- Customer TypeScript：通过
- Shared UI TypeScript：通过
- Customer ESLint：通过
- Shared UI ESLint：通过
- Customer production build：通过
- Git whitespace check：通过

