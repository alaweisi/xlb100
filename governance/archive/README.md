# Governance archive

本目录保存 2026-07-15 前的 Release Train、Work Unit、Manifest、Lease、Transition、Integration Queue、managed-worktree Gate、旧 Agent Skills 和相关验证记录。

这些内容仅用于历史追溯：

- 不产生当前施工权限或阻塞效力；
- 不得由 pre-commit、CI、`package.json`、preflight 或 Agent 默认流程调用；
- 当前治理只使用 `scripts/check-lean-risk.ps1` 和 `scripts/check-migration-integrity.ps1`；
- 只有未来团队确实扩大并明确决定重新设计多人并行治理时，才可把这里的经验作为参考，不能直接恢复旧状态机。

当前有效规则见仓库根目录 `AGENTS.md` 与 `governance/00_LEAN_EXECUTION_POLICY.md`。
